"""Naseej ML — modular pipeline (Phases 2–6).

This package lives alongside the original `ml/scripts/` pipeline, which remains
the source of truth for the existing trained `ml/models/baseline_model.pkl`.
"""
